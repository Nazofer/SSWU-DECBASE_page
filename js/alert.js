// const eventList = ['click', 'scroll', 'selectionchange'];
// const timeout = 60000;

// const resetTimer = () => {
//   clearTimeout(timer);
//   timer = setTimeout(showAlert, timeout);
// };

// const showAlert = () => {
//   confirm('Ви ще тут?') ? resetTimer() : window.close();
// };

// eventList.forEach(event => {
//   document.addEventListener(event, resetTimer);
// });

// let timer = setTimeout(showAlert, timeout);